import 'package:flutter/material.dart';
import 'package:velocity_x/velocity_x.dart';

class MakeBox extends StatelessWidget {
  const MakeBox({
    super.key,
  });

  @override
  Widget build(BuildContext context) => Container().box.amber500.size(20, 20).make();
}
